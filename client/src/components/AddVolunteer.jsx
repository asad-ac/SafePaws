import {useState} from 'react'

const AddVolunteer = (props) => {
    // boolean prop and setter to fetch id and add to total

    const [form, setForm] = useState({name: '', address: '', phone: '', email: '', assigned_duty: ''})
  
    return (
    <div>
      
    </div>
  )
}

export default AddVolunteer
